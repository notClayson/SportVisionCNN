# SportVisionCNN
A convolutional neural network implementation for classifying sports images. Trained on a custom dataset of 655 images across 4 sport categories using TensorFlow/Keras. The model architecture uses multiple Conv2D + MaxPooling layers followed by dense layers with softmax output.
